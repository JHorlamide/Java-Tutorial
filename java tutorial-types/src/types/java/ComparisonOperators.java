package types.java;

public class ComparisonOperators {
    public static void main(String[] args) {
        // Comparison Operators
        int x = 1;
        int y = 1;
        boolean isValueEqual = x == y;
        // System.out.println(isValueEqual);

        // Logical Operator
        int temperature = 30;
        boolean isWorm = temperature > 20 && temperature < 40;

        System.out.println(isWorm);
    }
}
